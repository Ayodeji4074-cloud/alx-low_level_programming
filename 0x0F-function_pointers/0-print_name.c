#include <stdio.h>
#include "function_pointers.h"

/**
 * print_name - it prints a name
 * @name: Name to print
 * @f: A pointer that points to a function printing a name
 */

char print_name(char);

void main()
{
	char name;
	char(*f)=&print_name
}

if(f==NULL)
	return (f(name));

